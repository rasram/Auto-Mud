# common/

**Owner:** Shared

Shared Python utilities used by more than one module, such as logging, config loading, and common data types (device IDs, flow records, anomaly scores). Keep this small. Code used by only one module belongs in that module.
