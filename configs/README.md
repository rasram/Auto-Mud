# configs/

**Owner:** Shared

Shared configuration used across modules, such as the 60s window length, anomaly thresholds and response tiers, Neo4j connection settings, and device-type definitions. Don't commit secrets. Use environment variables or a git-ignored `.env`.
