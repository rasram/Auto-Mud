# scripts/

**Owner:** Shared

Cross-cutting helper scripts that don't belong to a single module, such as environment setup, dataset download/verification, and running the full pipeline locally. Module-specific scripts belong inside that module's directory.
