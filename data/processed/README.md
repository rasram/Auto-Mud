# data/processed/

**Owner:** Shared (written mainly by `profiling/features/` and `ml/dataset_prep/`)

Derived data: Zeek flow logs from offline replay, per-device feature tables, and labeled graph snapshots. Everything here should be reproducible from `data/raw/` using the repo's code.
