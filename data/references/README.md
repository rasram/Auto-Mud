# Evaluation references

`mudgee_muds/` is the single committed MUDgee ACL reference corpus used for
UNSW Objective 1. The generators do not read these files while building
profiles; validation reads them after generation.
