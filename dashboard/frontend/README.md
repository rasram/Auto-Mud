# dashboard/frontend/

**Owner:** Rashwanth Ram

React frontend. Its JavaScript dependencies will be managed separately with its own `package.json` when this module is built. They are not part of `requirements.txt`.
